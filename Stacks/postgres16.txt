services:
  postgres:
    # **** >>> Agora usamos esta versão do postgres, está certo! <<< **** 
    image: pgvector/pgvector:pg16
    # **** >>> Agora usamos esta versão do postgres, está certo! <<< ****
    environment:
      - POSTGRES_PASSWORD=0bdhs1bwdcj0ahlmdfjw1y5bn
      - TZ=America/Sao_Paulo      
    networks:
      - minha_rede    
    volumes:
      - postgres_data:/var/lib/postgresql/data
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager         
      resources:
        limits:
          cpus: "1"
          memory: 2096M

volumes:
  postgres_data:
    external: true
    name: postgres_data

networks:
  minha_rede:
    external: true
    name: minha_rede